import { Injectable } from '@nestjs/common';

@Injectable()
export class AntibioticApiService {
  constructor() {} // private readonly _plateDelete: PlateDeleteService, // private readonly _plateUpdater: PlateUpdaterService, // private readonly _plateGetter: AntibioticeGetterService, // private readonly _plateCreator: AntibioticCreatorService,

  async create() {
    // const raw = await this._plateCreator.create(data);
    // return new PlateDto(raw);
  }
}
